{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
      {
        "label": "run",
        "detail": "This will install and launch app.",
        "type": "shell",
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "options": {
                "cwd": "${workspaceFolder}/bank"
            },
        "command": "mvn clean install -DskipTests -DskipUiTests; cd ui; mvn javafx:run",
        "presentation": {
            "reveal": "always"
        }
      },
      {
        "label": "install",
        "detail": "This is set to install app before launching from Run and Debug.",
        "type": "shell",
        "options": {
                "cwd": "${workspaceFolder}/bank"
            },
        "command": "mvn clean install -DskipTests -DskipUiTests",
        "presentation": {
            "reveal": "always"
        }
      }
    ]
  }